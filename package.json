{
  "name": "proxmox-ve-mcp",
  "version": "0.1.0",
  "description": "API-token authenticated MCP access to the Proxmox VE HTTP API",
  "license": "MIT",
  "type": "module",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/trac3r00/proxmox-mcp.git"
  },
  "homepage": "https://github.com/trac3r00/proxmox-mcp#readme",
  "bugs": {
    "url": "https://github.com/trac3r00/proxmox-mcp/issues"
  },
  "scripts": {
    "start": "bun src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun --packages external",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "engines": { "bun": ">=1.3.0" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.0",
    "ky": "^1.14.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.3"
  }
}
